package com.getbetterhub.app

import android.app.Application

/**
 * App entry point. Real builds would initialise the Room database and the Retrofit client here;
 * kept minimal so the Compose screens can be wired to real data sources in Part 2.
 */
class GetBetterHubApp : Application()
